# The server creates one single object adapter with the name
# "Query". The following line sets the endpoints for this
# adapter.
#
Query.Endpoints=tcp -p 10800
#
# Listen on all available interfaces
#
Ice.Default.Host=localhost